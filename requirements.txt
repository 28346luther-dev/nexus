# Nexus runs on the Python standard library alone — there is nothing to install.
# This file exists so build systems (Railway/Nixpacks, Render, Heroku) detect
# the project as Python. Leave it in place even though it lists no packages.
